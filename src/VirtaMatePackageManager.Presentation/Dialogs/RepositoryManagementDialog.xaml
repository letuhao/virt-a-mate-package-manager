<Window x:Class="VirtaMatePackageManager.Presentation.Dialogs.RepositoryManagementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VirtaMatePackageManager.Presentation.Dialogs"
        mc:Ignorable="d"
        Title="Repository Management" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Repository List -->
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Padding="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Repositories" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding Repositories}"
                             SelectedItem="{Binding SelectedRepository}"
                             DisplayMemberPath="Name"/>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="LightGray"/>

            <!-- Repository Details Form -->
            <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Repository Details" FontWeight="Bold" FontSize="14" Grid.Row="0" Margin="0,0,0,10"/>

                    <Grid Grid.Row="1" Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Name:" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding NewRepositoryName, UpdateSourceTrigger=PropertyChanged}"
                                 VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Grid.Row="2" Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Path:" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding NewRepositoryPath, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,5,0"
                                 VerticalAlignment="Center"/>
                        <Button Grid.Column="2" 
                                Content="Browse..." 
                                Command="{Binding BrowsePathCommand}"
                                Padding="10,2"
                                VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Grid.Row="3" Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Description:" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,5,0"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding NewRepositoryDescription, UpdateSourceTrigger=PropertyChanged}"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 Height="60"
                                 VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Grid.Row="4" Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Priority:" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding NewRepositoryPriority, UpdateSourceTrigger=PropertyChanged}"
                                 VerticalAlignment="Center"/>
                    </Grid>

                    <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Add" 
                                Command="{Binding AddRepositoryCommand}"
                                Padding="15,5"
                                Margin="0,0,5,0"
                                MinWidth="80"/>
                        <Button Content="Update" 
                                Command="{Binding UpdateRepositoryCommand}"
                                Padding="15,5"
                                Margin="0,0,5,0"
                                MinWidth="80"/>
                        <Button Content="Delete" 
                                Command="{Binding DeleteRepositoryCommand}"
                                Padding="15,5"
                                Margin="0,0,5,0"
                                MinWidth="80"/>
                    </StackPanel>

                    <StackPanel Grid.Row="6" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,10,0,0">
                        <Button Content="Enable/Disable" 
                                Command="{Binding EnableRepositoryCommand}"
                                Padding="15,5"
                                MinWidth="120"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="1" Background="#E0E0E0" Margin="0,5,0,0">
            <TextBlock Text="{Binding StatusMessage}"/>
        </StatusBar>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0">
            <Button Content="Refresh" 
                    Command="{Binding RefreshCommand}"
                    Padding="15,5"
                    Margin="0,0,5,0"
                    MinWidth="80"/>
            <Button Content="Close" 
                    IsCancel="True"
                    Padding="15,5"
                    MinWidth="80"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

